<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Verify Email - IT Logbook Supervisor</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #f9fcff;
      min-height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .login-container {
      background: #fff;
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(44,62,80,0.08);
      padding: 48px 48px 40px 48px;
      max-width: 410px;
      width: 100%;
      margin: 40px 0;
      text-align: center;
    }
    .logo {
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 28px;
    }
    .logo-icon {
      background: #1E379B;
      border-radius: 8px;
      width: 48px;
      height: 48px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-size: 28px;
      margin-right: 16px;
    }
    .logo-title h1 {
      font-size: 22px;
      font-weight: 600;
      color: #1E379B;
      margin: 0 0 2px 0;
    }
    .logo-title p {
      margin: 0;
      font-size: 14px;
      color: #1E379B;
      font-weight: 400;
      letter-spacing: .01em;
    }
    .main-heading {
      margin: 15px 0 0 0;
      font-size: 1.27rem;
      font-weight: 600;
      color: #1E379B;
      text-align: center;
    }
    .desc {
      font-size: 1.03rem;
      color: #374151;
      margin: 5px 0 22px 0;
      text-align: center;
      line-height: 1.45;
    }
    form {
      margin-top: 12px;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .form-group {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      width: 100%;
      margin-bottom: 10px;
    }
    .form-group label {
      font-size: 14px;
      color: #1E379B;
      margin-bottom: 6px;
      font-weight: 500;
    }
    .input-wrapper {
      position: relative;
      width: 100%;
    }
    .input-icon {
      position: absolute;
      left: 12px;
      top: 50%;
      transform: translateY(-50%);
      color: #94a3b8;
      pointer-events: none;
    }
    input[type="email"] {
      width: 100%;
      padding: 12px 16px 12px 42px;
      border: 1px solid #cbd5e1;
      border-radius: 8px;
      font-size: 14px;
      outline: none;
      background: #f7fafe;
      color: #222;
      box-sizing: border-box;
      transition: border .2s, box-shadow .2s;
    }
    input[type="email"]:focus {
      border-color: #1E379B;
      background: #fff;
      box-shadow: 0 0 0 2px rgba(30, 55, 155, 0.12);
    }
    input[type="email"]::placeholder {
      color: #b2b8c2;
      font-size: 15px;
      opacity: 1;
    }
    button {
      background-color: #1E379B;
      color: #ffffff;
      padding: 13px;
      font-size: 16px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 600;
      margin: 0;
      width: 100%;
      transition: background-color 0.2s ease;
    }
    button:hover {
      background-color: #162e80;
    }

    .alert-danger {
      background-color: #e13c4d;
      color: #fff;
      padding: 12px 16px;
      border-radius: 8px;
      font-size: 14px;
      margin-bottom: 20px;
      text-align: left;
    }

    @media (max-width: 600px) {
      .login-container { padding: 24px 6px 24px 6px; }
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="logo">
      <div class="logo-icon">📒</div>
      <div class="logo-title">
        <h1>IT Logbook</h1>
        <p>Supervisor Panel</p>
      </div>
    </div>
    <div class="main-heading">Forgot Password</div>
    <div class="desc">Enter your email address to receive a verification code.</div>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert-danger" role="alert"><%= error %></div>
    <% } %>

    <form id="loginForm" action="/forgot-password" method="POST" autocomplete="off">
      <div class="form-group">
        <label for="email">Email</label>
        <div class="input-wrapper">
          <span class="input-icon">
            <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.7" viewBox="0 0 20 20">
              <rect x="3" y="4" width="14" height="12" rx="2" stroke="currentColor"/>
              <path d="M3 6.5l7 4.5 7-4.5" stroke="currentColor"/>
            </svg>
          </span>
          <input type="email" name="email_address" id="email" placeholder="Enter your email address" required />
        </div>
      </div>
      <button type="submit">Verify</button>
    </form>
  </div>
<script>
  document.getElementById("loginForm").addEventListener("submit", (e) => {
    const email = document.getElementById("email").value.trim();
    if (!email) {
      alert("Please enter your email address.");
      e.preventDefault();
      return;
    }
  });
</script>
</body>
</html>
