<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>IT Supervisor Email Address Validation</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    :root {
      --blue-900: #1e3a8a;
      --blue-900-dark: #1a336f;
      --gray-100: #f3f4f6;
      --gray-700: #374151;
      --gray-300: #d1d5db;
      --orange-500: #f97316;
      --orange-600: #ea580c;
    }

    body {
      background-color: var(--gray-100);
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    .container {
      width: 100%;
      max-width: 600px;
      padding: 15px;
    }

    .blue-heading {
      background-color: var(--blue-900);
      color: white;
      padding: 15px;
      border-radius: 5px;
      text-align: center;
    }

    .form-wrapper {
      background: white;
      padding: 20px;
      border-radius: 8px;
      max-height: 90vh;
      overflow-y: auto;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .form-control {
      border: 1px solid var(--gray-300);
      box-shadow: none;
    }

    .form-control:focus {
      border-color: var(--blue-900);
      box-shadow:  0 0 0 2px #f97316;
    }

    .btn-custom {
      background-color: var(--orange-500);
      color: white;
      border: none;
      font-weight: 600;
      padding: 0.5rem 1.5rem;
      border-radius: 0.375rem;
      transition: background-color 0.3s ease;
    }

    .btn-custom:hover {
      background-color: var(--orange-600);
      color: white;
    }

    label {
      font-weight: 500;
      margin-bottom: 4px;
      color: var(--gray-700);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="blue-heading mb-4">
      <h2 class="mb-0">IT Supervisor Email Address Validation</h2>
      <p class="mb-0">Enter the OTP sent to <%= email %></p>
    </div>

    <% if (typeof error !== 'undefined' && error) { %>
    <div class="alert alert-danger" role="alert"><%= error %></div>
    <% } %>

    <div class="form-wrapper">
      <form action="/verify-otp" method="POST" class="bg-white p-4 rounded shadow-sm" novalidate>
        <input type="hidden" name="email" value="<%= email %>" />

        <div class="mb-3">
          <label for="otp" class="form-label">OTP</label>
          <input 
            type="text" 
            class="form-control" 
            id="otp" 
            name="otp" 
            required 
            pattern="\d{6}" 
            maxlength="6" 
            placeholder="Enter the 6-digit OTP"
            title="Please enter the 6-digit OTP sent to your email"
            autocomplete="one-time-code"
          />
        </div>

        <div class="text-end">
          <button type="submit" class="btn btn-custom">Verify</button>
        </div>
      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
