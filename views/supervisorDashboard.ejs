<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Supervisor Dashboard</title>
  <meta name="viewport" content="width=device-width,initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-blue-50 font-sans min-h-screen flex flex-col md:flex-row">

  <aside class="bg-blue-900 text-white w-full md:w-64 p-6 flex flex-col items-center">
    <div class="bg-white text-blue-900 rounded-full p-4 mb-4">
      <span class="text-2xl font-bold">👤</span>
    </div>
    <h2 class="text-xl font-bold"><%= user.Name%></h2>
    <p class="text-sm mb-6"><%= user.Email_Address%></p>
    <nav aria-label="Primary" class="flex flex-col h-full w-full text-left">
      <div class="space-y-4">
        <a href="/supervisorDashboard" class="block hover:text-blue-300 focus:outline-none focus:text-blue-300">📊 Dashboard</a>
        <a href="/students" class="block hover:text-blue-300 focus:outline-none focus:text-blue-300">👥 Students</a>
        <a href="/calendar" class="block hover:text-blue-300 focus:outline-none focus:text-blue-300">🗓️ Calendar</a>
        <a href="/notifications" class="block hover:text-blue-300">
         🔔 Notifications
         <% if (hasUnreadNotifications) { %>
          <span class="text-red-500 ml-2 font-bold">●</span>
          <% } %>
      </a>
        <a href="/settings" class="block hover:text-blue-300 focus:outline-none focus:text-blue-300">⚙️ Settings</a>
      </div>
      <div class="flex-grow"></div>
      <div>
        <a href="/logout" class="block hover:text-blue-300 focus:outline-none focus:text-blue-300">🚪 Logout</a>
      </div>
    </nav>
  </aside>

  <main class="flex-1 p-8 overflow-y-auto">
    <h1 class="text-2xl font-bold text-blue-900 mb-6">Dashboard Overview</h1>

    <form method="get" class="mb-6 relative w-full md:w-1/2"></form>
      <div class="grid grid-cols-2 gap-4 justify-center">

  <div class="w-50 h-48">
  <a href="/students" class="bg-white w-full h-full p-6 rounded-xl shadow text-blue-900 flex items-center gap-x-4 no-underline hover:bg-blue-100 transition duration-200">
    <div class="text-4xl">👥</div>
    <div class="flex flex-col">
      <span class="text-3xl font-bold">
        <% if (studentCount === 0) { %>
          No Students Assigned
        <% } else { %>
          <%= studentCount %>
        <% } %>
      </span>
      <% if (studentCount > 0) { %>
        <span class="text-sm">
          <%= studentCount === 1 ? 'Student Assigned' : 'Students Assigned' %>
        </span>
      <% } %>
    </div>
  </a>
</div>

  <div class="w-50 h-48">
    <a href="/calendar" class="bg-white w-full h-full p-6 rounded-xl shadow text-blue-900 flex items-center gap-x-4 no-underline hover:bg-blue-100 transition duration-200">
      <div class="text-4xl">🗓️</div>
      <div class="flex flex-col">
        <span class="text-3xl font-bold">Calendar</span>
        <span class="text-sm">View</span>
      </div>
    </a>
  </div>

 <div class="w-50 h-48">
    <a href="/notifications" class="bg-white w-full h-full p-6 rounded-xl shadow text-blue-900 flex items-center gap-x-4 no-underline hover:bg-blue-100 transition duration-200">
      <div class="text-4xl">🔔</div>
      <div class="flex flex-col">
        <span class="text-3xl font-bold">
          <% if (notificationCount === 0) { %>
            No Notifications
            <% } else { %>
            <%= notificationCount %>
          <% } %>
        </span>
        <% if (notificationCount > 0) { %>
          <span class="text-sm">
            <%= notificationCount === 1 ? 'Notification' : 'Notifications' %>
          </span>
        <% } %>
      </div>
    </a>
  </div>

 <div class="w-50 h-48">
    <a href="/settings" class="bg-white w-full h-full p-6 rounded-xl shadow text-blue-900 flex items-center gap-x-4 no-underline hover:bg-blue-100 transition duration-200">
      <div class="text-4xl">⚙️</div>
      <div class="flex flex-col">
        <span class="text-3xl font-bold">Settings</span>
        <span class="text-sm">View Information</span>
      </div>
    </a>
  </div>
  </main>
</body>
</html>